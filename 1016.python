import sys

min_val, max_val = map(int, sys.stdin.readline().split())

is_sq_free = [True] * (max_val - min_val + 1)

i = 2
while i * i <= max_val:
    square = i * i
    
    start_index = (min_val // square) * square
    if start_index < min_val:
        start_index += square
        
    for j in range(start_index, max_val + 1, square):
        is_sq_free[j - min_val] = False
    i += 1

print(sum(is_sq_free))
